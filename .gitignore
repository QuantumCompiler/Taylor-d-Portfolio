*.DS_STORE
.DS_Store

# Build-time secrets — never commit. Copy lib/secrets/Secrets.example.xcconfig to
# lib/secrets/Secrets.xcconfig and fill in your Adzuna credentials locally.
Secrets.xcconfig

# Xcode per-developer UI state (not shared).
*.xcuserstate
