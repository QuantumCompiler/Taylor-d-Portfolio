// Presentation · Landing · ViewModel
// LandingViewModel — @MainActor @Observable. The landing screen is currently
// static, so there's no view state yet; this will own the "Get Started" intent
// once it routes into the Search flow.
// Placeholder to keep this folder in git — delete once LandingViewModel lands here.
